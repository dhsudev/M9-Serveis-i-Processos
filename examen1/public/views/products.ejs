<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lua's products</title>
    <link rel="stylesheet" href="../styles.css">
</head>
<body class="container">
    <header>
        <h1>Gestió de productes per <%= user.name %>!</h1>
        <h2><%- htmlMessage %></h2>
    </header>
    <main>
        <section>
            <h2 class="section-title">Llista de productes</h2>
            <div class="product-card-container">
                <% products.forEach(element => { %>
                    <div class="product-card">
                        <h2><%= element.nom %></h2>
                        <p>Categoria: <%= element.categoria %> | Preu: <%= element.preu %></p>
                    </div>
                <% }); %>
            </div>
        </section>
    </main>
    <footer>
        <p>&copy; 2025 Lua's Products</p>
    </footer>
</body>
</html>